[project]
name = "subconv"
version = "2.0.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi==0.111.0",
    "httpx==0.27.0",
    "pydantic-settings>=2.4.0",
    "pyyaml>=6.0",
    "uvicorn==0.44.0",
]

[dependency-groups]
build = [
    "nuitka[onefile]",
]

[tool.uv]
package = false
